# Drizzle migration snapshots are generated by drizzle-kit (full-schema dumps,
# not hand-edited). Mark them generated AND non-diffable so GitHub collapses
# them, drops them from the language stats, and excludes them from the PR's
# additions/deletions count. Git/CI/drizzle ignore these attributes.
src/shared/lib/db/migrations/meta/** linguist-generated=true -diff
