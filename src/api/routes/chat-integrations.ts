/** Legacy URL entry point; all management behavior lives in the canonical router. */
// TODO(2026-12-01): Delete this compatibility entry point with the /api/chat-integrations alias.
export { default } from './agent-integrations'
