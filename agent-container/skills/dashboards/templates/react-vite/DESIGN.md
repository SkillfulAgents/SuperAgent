---
name: Dashboard
extends: ~/.claude/skills/dashboards/DESIGN.md
# Override any token below to customize this dashboard. Anything not overridden
# inherits from the system DESIGN.md. After editing tokens here, update the
# matching values in src/tokens.css — the two files must stay in sync.
#
# Example:
# colors:
#   primary: "#3b82f6"
# rounded:
#   lg: 12px
---

## Overview

This dashboard inherits the Superagent design system. See the system
[DESIGN.md](file:~/.claude/skills/dashboards/DESIGN.md) for the full token
reference, color rules, type scale, and component recipes.

## Local Notes

Use this section to capture decisions specific to this dashboard — e.g., why a
particular chart palette was chosen, or which custom components exist and when
to reach for them.

(Delete this placeholder once the dashboard has its own context to record.)
