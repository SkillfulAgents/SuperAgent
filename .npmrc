# Supply-chain cooldown: refuse package versions published less than 7 days ago
# (mitigates registry account-takeover / package-poisoning attacks).
# Requires npm >= 11.10.0 — older npm silently ignores this setting.
min-release-age=7
